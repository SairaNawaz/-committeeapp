<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white">

    <TextView
        style="@style/bodyStyleBold1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="No People..." />

    <android.support.v7.widget.RecyclerView
        android:id="@+id/members_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/btn_redraw"
        android:layout_below="@+id/title"
        android:background="@color/white"
        style="@style/ScrollBarStyle"></android.support.v7.widget.RecyclerView>

    <TextView
        android:id="@+id/btn_redraw"
        style="@style/generalButtonStyle"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:text="@string/re_draw" />

    <TextView
        android:id="@+id/btn_sharewithmembers"
        style="@style/generalButtonStyle"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:layout_toLeftOf="@+id/btn_redraw"
        android:text="@string/share_with_members" />
</RelativeLayout>
